{% extends 'base.html.twig' %}

{% block title %}Editer Mairie{% endblock %}

{% block body %}
    <h1>Editer Mairie</h1>
    <div class="col-md-2">
    {{ include('mairie/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
    <a class="btn btn-light" href="{{ path('mairie_index') }}"> retour a la liste </a>

    {{ include('mairie/_delete_form.html.twig') }}
{% endblock %}
