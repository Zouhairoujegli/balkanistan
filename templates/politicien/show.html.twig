{% extends 'base.html.twig' %}

{% block title %}Politicien{% endblock %}

{% block body %}
    <h1>Politicien</h1>
    <div  class="col-md-4">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ politicien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ politicien.nom }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ politicien.sexe }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ politicien.age }}</td>
            </tr>
            </tr>
            <tr>
                <th>Actions</th>
                <td>
                    <div class="row">
                        <div class="col">
                              <a class="btn btn-primary mr-4" href="{{ path('politicien_edit', {'id': politicien.id}) }}">editer</a>
                        </div>
                        <div class="col">
                            {{ include('politicien/_delete_form.html.twig') }}
                        </div>
                    </div>
                </td>
            </tr>
        </tbody>
    </table>
    </div>

    <a class="btn btn-light" href="{{ path('politicien_index') }}">retour a la liste</a>

{% endblock %}
