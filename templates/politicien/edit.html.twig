{% extends 'base.html.twig' %}

{% block title %}Edit Politicien{% endblock %}

{% block body %}
    <h1>Editer Politicien</h1>
    <div class="col-md-2">
    {{ include('politicien/_form.html.twig', {'button_label': 'Modifier'}) }}   
    </div>
    <a class="btn btn-light" href="{{ path('politicien_index') }}">retour a la liste</a>
  
    {{ include('politicien/_delete_form.html.twig') }}
{% endblock %}
