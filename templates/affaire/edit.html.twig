{% extends 'base.html.twig' %}

{% block title %}Edit Affaire{% endblock %}

{% block body %}
    <h1>Editer Affaire</h1>
    <div class="col-md-2">
    {{ include('affaire/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
    <a class="btn btn-light" href="{{ path('affaire_index') }}"> retour a la liste </a>

    {{ include('affaire/_delete_form.html.twig') }}
    
{% endblock %}
